def punctuation() -> str:
    """Возвращает строку с распространёнными знаками препинания."""
    print('функция punctuation вызвана')
    return '.,:;!?-–—\'\"()'


punctuation()
puncs = punctuation()
